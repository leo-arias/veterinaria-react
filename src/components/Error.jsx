const Error = ({children}) => {
  return (
    
    <div className="bg-red-700 text-white uppercase p-3 text-center font-bold mb-3 rounded-lg">
        {children}
    </div>

  )
}

export default Error